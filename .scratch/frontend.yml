agent: frontend
session_id: ext-auth-2026-05-11
last_updated: "2026-05-19T13:30:00Z"
status: active

current_task: ISS-003 complete — background handler Clerk JWT plumbing

tasks:
  frontend-types-fix:
    description: Fix TypeScript type mismatches in ui/components, sdk/core caused by updated TensionRating, SourceResult, and AnalyzeResponse shapes in @shirajitsu/types
    status: complete
    started_at: "2026-05-06T00:00:00Z"
    completed_at: "2026-05-06T00:00:00Z"
    output: ".handoffs/frontend-types-fix.md"
    notes: |
      Files changed:
        - ui/components/src/annotation/AnnotationCard.test.tsx
            - TensionRating mock: numerator -> score, denominator -> sourceCount
            - SourceResult mock: added relevanceScore: 0.95, divergenceScore: null
        - sdk/core/src/client.test.ts
            - AnalyzeResponse mock: added failedClaims: [], sessionStats with zeros and default model/provider

      Typecheck result: pnpm typecheck from repo root — 7 successful, 0 failed.

      Packages verified clean:
        - @shirajitsu/types (no changes)
        - @shirajitsu/react (ui/components) — was failing, now passing
        - @shirajitsu/core (sdk/core) — was failing, now passing
        - @shirajitsu/web — was already passing
        - @shirajitsu/extension — was already passing

      Decision DEC-001 logged: mock data alignment in test files; no assertions changed.
      Blocker from orchestrator.yml resolved: "TypeScript type mismatches blocking extension, web, and SDK CI"

  extension-error-visibility-fix:
    description: Fix silent failure when clicking "Analyze this page" — errors not shown in popup or sidebar; content script unavailable path caused unchecked TypeError
    status: complete
    started_at: "2026-05-11T15:20:00Z"
    completed_at: "2026-05-11T15:29:00Z"
    output: null
    notes: |
      Files changed:
        - ui/extension/src/popup/Popup.tsx
            - handleAnalyze(): added chrome.runtime.lastError check and res === undefined guard
            - broadcastError() helper: sets popup error state + sends SHOW_ERROR via chrome.runtime.sendMessage
            - Added chrome.runtime.sendMessage broadcast for SHOW_ANNOTATIONS (sidebar channel)
            - GET_CONTEXT callback: added chrome.runtime.lastError guard
        - ui/extension/src/sidebar/Sidebar.tsx
            - Added 'error' to status state union
            - Added errorMsg state
            - Added SHOW_ERROR message handler
            - Rendered sidebar__error paragraph with role="alert"
            - Scoped annotation card render to status === 'done'
        - ui/extension/src/styles/sidebar.css
            - Added .sidebar__error rule (same padding as .sidebar__empty, color: var(--color-risk-high))

      Build: pnpm --filter @shirajitsu/extension build — PASS
      Tests: pnpm --filter @shirajitsu/extension test — 6/6 PASS
      Decision DEC-006 logged: chrome.runtime.sendMessage vs chrome.tabs.sendMessage for popup-to-sidebar

  extension-unguarded-tabmessage-fix:
    description: Audit all four extension source files for unguarded chrome.runtime.sendMessage / chrome.tabs.sendMessage calls; fix the unguarded SHOW_ANNOTATIONS chrome.tabs.sendMessage in Popup.tsx that was the persistent root cause of "Receiving end does not exist" after 0d22584
    status: complete
    started_at: "2026-05-11T16:15:00Z"
    completed_at: "2026-05-11T16:24:00Z"
    output: "ui/extension/src/popup/Popup.tsx"
    notes: |
      Root cause: chrome.tabs.sendMessage(tab.id!, { type: 'SHOW_ANNOTATIONS', ... }) at Popup.tsx line 96
      had NO callback. Chrome raises "Receiving end does not exist" when the content script is absent
      (chrome:// pages, PDF viewer, etc.) and no callback is provided to read chrome.runtime.lastError.

      Fix: introduced safeTabMessage() helper (analogous to safeBroadcast) that calls chrome.tabs.sendMessage
      with a callback that voids chrome.runtime.lastError. Replaced the unguarded call with safeTabMessage.

      Build: pnpm --filter @shirajitsu/extension build — PASS (tsc + vite, 0 errors)
      Tests: pnpm --filter @shirajitsu/extension test — 6/6 PASS
      Decision DEC-010 logged.

  ISS-001-clerk-provider-popup-entrypoint:
    description: "Wrap popup entrypoint (main.tsx) with ClerkProvider from @clerk/chrome-extension; validate VITE_CLERK_PUBLISHABLE_KEY at startup"
    status: complete
    started_at: "2026-05-15T15:50:00Z"
    completed_at: "2026-05-15T16:10:00Z"
    output: ".handoffs/frontend-completion-ISS-001.md"
    notes: |
      Files changed:
        - ui/extension/src/popup/main.tsx
            - Added ClerkProvider from @clerk/chrome-extension
            - Added startup invariant: throws descriptive Error if VITE_CLERK_PUBLISHABLE_KEY
              is missing or does not start with 'pk_'
            - Wrapped <Popup /> in <ClerkProvider publishableKey={...}> inside <StrictMode>
        - ui/extension/tsconfig.json
            - Added "vite/client" to types array for import.meta.env type support (DEC-017)

      Typecheck result: ISS-001-introduced error resolved. Pre-existing errors in
      handler.test.ts and Popup.test.tsx remain (not ISS-001 scope).

      Test result: 39 failed | 9 passed (48 total) — no regressions vs phase-1 baseline.
      TC-001 through TC-013 still fail (require ISS-002 — Popup.tsx conditional rendering).

      Decisions: DEC-017 logged.
      Completion artifact: .handoffs/frontend-completion-ISS-001.md

  ISS-003-handler-clerk-jwt-plumbing:
    description: "Replace legacy userToken storage read in handler.ts with live Clerk getToken() call; initialize background Clerk instance in background/index.ts"
    status: complete
    started_at: "2026-05-19T12:30:00Z"
    completed_at: "2026-05-19T13:30:00Z"
    output: ".handoffs/frontend-completion-ISS-003.md"
    notes: |
      Files changed:
        - ui/extension/src/background/handler.ts
            - Added import of __unstable__createClerkClient from @clerk/chrome-extension/background
            - Module-level clerkPromise initialization with VITE_CLERK_PUBLISHABLE_KEY
            - Changed handleAnalyze signature: (message, sendResponse) instead of (request)
            - Removed chrome.storage.sync.get(['gatewayUrl', 'userToken']) pattern
            - Removed const token = settings.userToken
            - Added: const clerk = await clerkPromise; const token = await clerk.session?.getToken() ?? null
            - Null token path calls sendResponse({ error: 'Not authenticated...' }) and returns
            - gatewayUrl storage read preserved using callback form wrapped in Promise (DEC-023)
        - ui/extension/src/background/index.ts
            - Updated handleAnalyze call to new (message, sendResponse) signature
            - Removed .then(sendResponse) chain; now handleAnalyze takes sendResponse directly
        - ui/extension/.env.test (new)
            - VITE_CLERK_PUBLISHABLE_KEY=pk_test_placeholder_for_testing_only
            - Required for TC-017-a publishable key regex assertion (DEC-024)

      Targeted test cases:
        - TC-014-B (3 tests): session null path — auth error, no fetch
        - TC-015 (4 tests): JWT in Authorization header
        - TC-016 (3 tests): userToken not in source files
        - TC-018 (3 tests): getToken() null path — auth error, no fetch
        - TC-019 (1 test): gatewayUrl from storage still works
        - TC-017 (2 tests): Clerk init with publishable key, ordering before listener
        - TC-020 (2 tests): module loads clean, addListener called

      Expected result: 15 previously failing tests now pass; 33 prior passing tests unaffected.
      Decisions: DEC-023, DEC-024 logged.
      Completion artifact: .handoffs/frontend-completion-ISS-003.md

blockers: []
