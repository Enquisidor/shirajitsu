# source-evaluator /evaluate — Gate Passed

**Session:** impl-phase1-2026-04-29
**Completed:** 2026-05-05T17:00:00Z
**Gate verified:** 2026-05-05T18:03:49Z

## Files delivered

| File | Status |
|---|---|
| `services/source-evaluator/internal/handler/evaluate.go` | Created |
| `services/source-evaluator/internal/handler/evaluate_test.go` | Created |
| `services/source-evaluator/cmd/server/main.go` | Rewritten (fully wired) |
| `services/source-evaluator/go.mod` | Updated (fsnotify v1.7.0) |
| `services/source-evaluator/go.sum` | Generated by go mod tidy |

## Implementation summary

- `EvaluateHandler` struct with injected `*registry.Loader`, `search.Provider`, `*paywall.Detector`, `*slog.Logger`
- Parallel goroutine fan-out per claim via `sync.WaitGroup` — latency scales with slowest single search, not total claims
- Per-result routing: `commentary.IsCommentary()` → commentaryItems; else `registry.ClassifyURL()` + `paywall.IsAccessible()` → sources
- Per-request provider override: when `searchProvider` field differs from default, constructs one-off provider from env keys
- All error paths return `{"error":"...","code":"..."}` with correct HTTP status (400 / 502 / 500)
- `main.go`: signal context (SIGTERM/SIGINT), registry hot-reload via `loader.Watch(ctx)`, weekly paywall recheck goroutine, port 8082

## Spec decisions logged

- **502 on total failure only**: partial search failure (one of N claims) → 200 with empty arrays for that claim; 502 only when every claim's search fails. Rationale: spec says "search API unavailable" → implies wholesale provider failure, not single-claim miss.
- **`anchorSourceUrl`**: set to same value as `sourceUrl` — no anchor concept in the search `Result` struct.
- **Port**: corrected from stub's 8080 default to 8082 per CLAUDE.md port table.

## Verification

```
go mod tidy   — PASS (fsnotify v1.7.0, chi v5.0.12, sys v0.4.0 downloaded)
go build ./... — PASS (clean, no errors)
go test ./...  — PASS

Packages tested:
  internal/domain   — 7 tests PASS (TestDetermineState, TestTier_GeneratesRating)
  internal/handler  — 5 tests PASS (TestHappyPath, TestEmptyClaimsArray,
                                     TestMalformedJSONBody, TestSearchProviderError,
                                     TestNoSearchResults)
  internal/registry — 11 tests PASS (TestClassifyURL)
  Total: 23 assertions, 0 failures
```

## Next task

backend-task2-annotator: implement annotator `/annotate` HTTP handler + LLM provider pattern + tension score computation.
Gate: `.handoffs/annotator-annotate.md` + `go test ./...` PASS.
Depends on: this task (COMPLETE).
