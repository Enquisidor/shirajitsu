name: source-evaluator

on:
  push:
    branches: [main]
    paths:
      - 'services/source-evaluator/**'
      - '.github/workflows/source-evaluator.yml'
  pull_request:
    paths:
      - 'services/source-evaluator/**'
      - '.github/workflows/source-evaluator.yml'
  workflow_dispatch:

jobs:
  build:
    name: Test & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Tidy
        working-directory: services/source-evaluator
        run: go mod tidy

      - name: Vet
        working-directory: services/source-evaluator
        run: go vet ./...

      - name: Test
        working-directory: services/source-evaluator
        run: go test ./...

      - name: Build binary
        working-directory: services/source-evaluator
        run: CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o source-evaluator ./cmd/server

      - uses: actions/upload-artifact@v4
        with:
          name: source-evaluator-${{ github.sha }}
          path: services/source-evaluator/source-evaluator
          retention-days: 7
