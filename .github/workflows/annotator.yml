name: annotator

on:
  push:
    branches: [main]
    paths:
      - 'services/annotator/**'
      - '.github/workflows/annotator.yml'
  pull_request:
    paths:
      - 'services/annotator/**'
      - '.github/workflows/annotator.yml'
  workflow_dispatch:

jobs:
  build:
    name: Test & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Tidy
        working-directory: services/annotator
        run: go mod tidy

      - name: Vet
        working-directory: services/annotator
        run: go vet ./...

      - name: Test
        working-directory: services/annotator
        run: go test ./...

      - name: Build binary
        working-directory: services/annotator
        run: CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o annotator ./cmd/server

      - uses: actions/upload-artifact@v4
        with:
          name: annotator-${{ github.sha }}
          path: services/annotator/annotator
          retention-days: 7
